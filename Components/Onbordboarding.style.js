import { StyleSheet } from 'react-native';
import { BASE_PADDING, GRAY_LIGHT } from '../styles';

export default StyleSheet.create({
  root: {
    flex: 1,
    ...BASE_PADDING,
    backgroundColor: GRAY_LIGHT,
  },
});
